{
  "name": "excel-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/index.html",
    "dev": "parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "powershell Remove-Item dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "constants-browserify": "^1.0.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "htm": "^3.1.1",
    "preact": "^10.11.0"
  }
}
